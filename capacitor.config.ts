import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.palladium.securechat',
  appName: 'Palladium Secure Chat',
  webDir: 'dist'
};

export default config;
